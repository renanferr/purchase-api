version: "2"
sql:
  - engine: "postgresql"
    queries: "queries"
    schema: "migrations"
    gen:
      go:
        package: "db"
        out: "internal/db"
        emit_db_tags: true
        emit_pointers_for_null_types: true
        sql_package: "pgx/v5"
